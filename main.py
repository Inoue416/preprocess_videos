from extractLips import *
import os
import env

def main():
    root = env.ROOT
    save_root = env.SAVE_ROOT
    if save_root is None:
        save_root = root
    err_log = env.ERR_LOG
    path_file = env.PATH_FILE
    not_use = env.NOT_USE
    kind1 = env.KIND1
    kind2 = env.KIND2
    

main()
